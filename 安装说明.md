# 听现 Lishon 国际版 · 安装说明

版本：0.7.2 · Windows 10 2004 及以上 / Windows 11，64 位。

## 一次安装软件与模型

双击本目录的 **Lishon-International-0.7.2-x64.exe**，按以下步骤操作：

1. 选择**软件安装位置**，可直接输入路径或浏览文件夹。
2. 在“语言包存放位置”选择模型目录。点击**与软件放在同一目录**可直接沿用软件目录，也可另选磁盘和文件夹。
3. 点击安装，等待软件和内置模型解压、校验完成，然后启动。

支持中文、空格路径。安装器使用实际选择的目录，不会再自动追加产品名称。所选目录需要可写，并有足够空间；安装时系统临时目录也需要空间，建议系统盘及目标盘各预留至少 8 GB。

| 安装方式 | 软件目录示例 | 模型目录示例 |
| --- | --- | --- |
| 同一目录 | `D:\Lishon` | `D:\Lishon` |
| 软件目录内的子目录 | `D:\Lishon` | `D:\Lishon\Models` |
| 不同目录或磁盘 | `D:\Apps\Lishon` | `E:\语言模型\Lishon` |

模型目录下会生成 `translation` 和 `speech` 文件夹。软件通过安装位置记录自动找到模型，无需首次启动后重新下载，也无需手工复制。安装过程中不会要求填写 API。

## 内置内容

- 听现国际版、桌面辅助程序、本地推理运行环境和词典数据。
- 英语与中文、日语、韩语、法语、德语、西班牙语、俄语的双向翻译模型，共 8 种语言、14 个直译方向。
- Whisper Base 语音识别模型，用于系统声音、麦克风及导入音视频的听译。

内置模型占据安装包的主要体积。安装包约 2.06 GiB，是包含这些模型的完整单文件安装器，无需额外下载模型分卷。上述本地模型安装和推理不依赖 VPN 或 API。其他没有本地直译模型的语言组合可使用 AI；不会自动经英语中转。部分屏幕识别语种仍需 Windows 对应的 OCR 语言功能。

## 首次使用与后续更新

在“语言与模型”中查看所选路径、翻译模型及语音识别包状态。关闭顶部 AI 开关即可使用本地模型；开启 AI 需要单独配置 API。

安装器会记住本次选择的模型目录。之后在应用内修改路径，重启也会保留；应用内更改路径仅切换读取位置，不会自动迁移旧文件。重新运行安装器时，新选择的模型位置会在首次启动时生效。

升级保留资料。卸载前可分别勾选删除本地翻译历史和本安装器记录的语言、语音模型；两项默认不勾选，仅删除软件。勾选后按清单删除相应文件，存储目录内的其他文件及个人语库仍保留。共用这些模型的其他应用也会受影响。国际版使用独立的 `lishon-international` 用户数据目录，不覆盖原中文版。

本安装器尚未进行发行者数字签名。安装包旁的 `.sha256` 文件用于核对文件是否完整；校验值不能替代发行者签名。

English: [Installation guide](docs/INSTALLATION.md)

## 0.7.2 本地翻译历史

进入“我的语库 → 本地翻译历史”，先选择保存文件夹；软件会在其中使用 `Lishon-History` 子目录。也可在“设置 → 处理与隐私”开关记录或更改保存位置。

屏幕字幕、实时听译会话超过 15 秒后自动保存为 JSONL 结构化记录，暂停时间不计入时长。原文及多种译文一起保存；“本地”指文件保存在电脑上，AI 与离线翻译结果均可记录。关闭记录不会删除旧文件。

双击记录名称修改标题；双击内容，在软件右侧打开同主题的阅读面板。右上角可切换逐条对照或按语种分段排列、导出、全屏或关闭。全屏保留当前排列方式，并将文字居中显示。PDF 使用白底黑色正文，语种提示使用中灰色；Markdown 使用内嵌 HTML 样式标注中灰色语种，具体显示取决于阅读器是否支持该样式。关闭阅读面板右上角的“显示序号和语种”后，阅读和导出均省略这些标注。阅读面板可拖离主窗口独立移动，靠近主窗口右边缘约 28 逻辑像素并松手时吸附。旧版 Markdown 历史仍可读取。文件夹按钮定位原始记录文件。删除或批量删除会同时删除对应文件。文件名包含稳定标识，重命名会同步修改文档标题。切换保存目录不会自动移动旧记录。
