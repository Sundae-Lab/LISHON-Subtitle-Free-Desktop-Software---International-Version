const {app,BrowserWindow}=require('electron');const path=require('path');
app.setPath('userData',path.join(__dirname,'../.qa/browser-fixture-data'));
app.whenReady().then(async()=>{const w=new BrowserWindow({width:960,height:420,title:'Lishon selection fixture — Chromium',webPreferences:{contextIsolation:true}});await w.loadURL('data:text/html,'+encodeURIComponent('<!doctype html><html><head><title>Lishon selection fixture — Chromium</title></head><body style="margin:60px;background:#fff;color:#111;font:24px/2 Arial"><p id="sample" style="white-space:nowrap;margin:0">The meeting starts tomorrow.</p><textarea id="edit" style="margin-top:30px;font:24px Arial;width:700px;height:70px">Welcome to the world.</textarea></body></html>'));w.show();w.focus();});app.on('window-all-closed',()=>app.quit());
